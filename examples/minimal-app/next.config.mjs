// Minimal Next.js config — the test exists to exercise rules_nextjs's
// next_build action, not to validate Next.js config behavior.
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
};

export default nextConfig;
